<h2>タスク一覧</h2>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task_path(task) %> [名前]：<%= task.status %> [内容]:<%= task.content %></li>
  <% end %>
</ul>

<%= link_to "タスク作成", new_task_path() %>