<h2>id:<%= @task.id %>タスク編集</h2>

<%= form_with(model: @task, local: true) do |f| %>
  <%= f.label :content, "変更内容" %>
  <%= f.text_field :content %>
  
  <%= f.submit "変更する" %>
<% end %>

<%= link_to "一覧に戻る", tasks_path() %>
<%= link_to "詳細に戻る", task_path(@task) %>